# Dependencies
node_modules/

# Environment / secrets — NEVER commit these
.env
.env.local
.env.*.local

# Firebase service-account key files (if ever downloaded locally)
*serviceAccount*.json
*service-account*.json
firebase-adminsdk*.json
*firebase*service*account*.json

# Logs
logs/
*.log
npm-debug.log*

# OS / editor
.DS_Store
Thumbs.db
